import { LinkHighlighterDirective } from './link-highlighter.directive';

describe('LinkHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
